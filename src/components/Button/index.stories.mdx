import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, boolean, select } from '@storybook/addon-knobs';
import { withA11y } from '@storybook/addon-a11y';

import {
  knobButtonLoading,
  knobButtonSizes,
} from '../../../.storybook/knobs/button';

import Button from '.';

<Meta title="Components/Buttons" component={Button} />

# Button

O Button pode ser utlizado para realizar ações. Podendo adicionar a propriedade `href` para usá-lo como uma tag `<a>`

## Variants

### Filled buttons

<Preview>
  <Story name="Filled" decorators={[withKnobs, withA11y]}>
    <div className="row-items">
      <Button isLoading={knobButtonLoading()}>Default</Button>
      <Button isLoading={knobButtonLoading()} color="primary">
        Primary
      </Button>
      <Button isLoading={knobButtonLoading()} color="secondary">
        Secondary
      </Button>
      <Button isLoading={knobButtonLoading()} bold>
        Bold
      </Button>
      <Button isLoading={knobButtonLoading()} disabled>
        Disabled
      </Button>
    </div>
  </Story>
</Preview>

### Outlined buttons

<Preview>
  <Story name="Outlined" decorators={[withKnobs, withA11y]}>
    <div className="row-items">
      <Button isLoading={knobButtonLoading()} variant="outlined">
        Default
      </Button>
      <Button
        isLoading={knobButtonLoading()}
        color="primary"
        variant="outlined"
      >
        Primary
      </Button>
      <Button
        isLoading={knobButtonLoading()}
        color="secondary"
        variant="outlined"
      >
        Secondary
      </Button>
      <Button isLoading={knobButtonLoading()} disabled variant="outlined">
        Disabled
      </Button>
    </div>
  </Story>
</Preview>

### Text buttons

<Preview>
  <Story name="Text" decorators={[withKnobs, withA11y]}>
    <div className="row-items">
      <Button isLoading={knobButtonLoading()} variant="text">
        Default
      </Button>
      <Button isLoading={knobButtonLoading()} color="primary" variant="text">
        Primary
      </Button>
      <Button isLoading={knobButtonLoading()} color="secondary" variant="text">
        Secondary
      </Button>
      <Button isLoading={knobButtonLoading()} disabled variant="text">
        Disabled
      </Button>
    </div>
  </Story>
</Preview>

## Shapes

### Round Square

<Preview>
  <Story name="Round-square" decorators={[withKnobs, withA11y]}>
    <div>
      <div className="row-items">
        <Button isLoading={knobButtonLoading()} shape="round-square">
          Round-square
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="primary"
          shape="round-square"
        >
          Round-square
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="secondary"
          shape="round-square"
        >
          Round-square
        </Button>
        <Button isLoading={knobButtonLoading()} shape="round-square" disabled>
          Round-square
        </Button>
      </div>
      <div className="row-items">
        <Button
          isLoading={knobButtonLoading()}
          shape="round-square"
          variant="outlined"
        >
          Round-square
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="primary"
          variant="outlined"
          shape="round-square"
        >
          Round-square
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="secondary"
          variant="outlined"
          shape="round-square"
        >
          Round-square
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          shape="round-square"
          variant="outlined"
          disabled
        >
          Round-square
        </Button>
      </div>
      <div className="row-items">
        <Button
          isLoading={knobButtonLoading()}
          shape="round-square"
          variant="text"
        >
          Round-square
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="primary"
          variant="text"
          shape="round-square"
        >
          Round-square
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="secondary"
          variant="text"
          shape="round-square"
        >
          Round-square
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          shape="round-square"
          variant="text"
          disabled
        >
          Round-square
        </Button>
      </div>
    </div>
  </Story>
</Preview>

### Circle buttons

<Preview>
  <Story name="Circle" decorators={[withKnobs, withA11y]}>
    <div>
      <div className="row-items">
        <Button
          isLoading={knobButtonLoading()}
          size={knobButtonSizes()}
          shape="circle"
        >
          @
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="primary"
          size={knobButtonSizes()}
          shape="circle"
        >
          @
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="secondary"
          size={knobButtonSizes()}
          shape="circle"
        >
          @
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          size={knobButtonSizes()}
          shape="circle"
          disabled
        >
          @
        </Button>
      </div>
      <div className="row-items">
        <Button
          isLoading={knobButtonLoading()}
          size={knobButtonSizes()}
          shape="circle"
          variant="outlined"
        >
          @
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="primary"
          variant="outlined"
          size={knobButtonSizes()}
          shape="circle"
        >
          @
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="secondary"
          variant="outlined"
          size={knobButtonSizes()}
          shape="circle"
        >
          @
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          size={knobButtonSizes()}
          shape="circle"
          variant="outlined"
          disabled
        >
          @
        </Button>
      </div>
      <div className="row-items">
        <Button
          isLoading={knobButtonLoading()}
          size={knobButtonSizes()}
          shape="circle"
          variant="text"
        >
          @
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="primary"
          variant="text"
          size={knobButtonSizes()}
          shape="circle"
        >
          @
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="secondary"
          variant="text"
          size={knobButtonSizes()}
          shape="circle"
        >
          @
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          size={knobButtonSizes()}
          shape="circle"
          variant="text"
          disabled
        >
          @
        </Button>
      </div>
    </div>
  </Story>
</Preview>

## Sizes

### Medium buttons

<Preview>
  <Story name="Medium" decorators={[withKnobs, withA11y]}>
    <div>
      <div className="row-items">
        <Button isLoading={knobButtonLoading()} size="medium">
          Default
        </Button>
        <Button isLoading={knobButtonLoading()} color="primary" size="medium">
          Primary
        </Button>
        <Button isLoading={knobButtonLoading()} color="secondary" size="medium">
          Secondary
        </Button>
        <Button isLoading={knobButtonLoading()} size="medium" disabled>
          Disabled
        </Button>
      </div>
      <div className="row-items">
        <Button
          isLoading={knobButtonLoading()}
          size="medium"
          variant="outlined"
        >
          Default
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="primary"
          variant="outlined"
          size="medium"
        >
          Primary
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="secondary"
          variant="outlined"
          size="medium"
        >
          Secondary
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          size="medium"
          variant="outlined"
          disabled
        >
          Disabled
        </Button>
      </div>
      <div className="row-items">
        <Button isLoading={knobButtonLoading()} size="medium" variant="text">
          Default
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="primary"
          variant="text"
          size="medium"
        >
          Primary
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="secondary"
          variant="text"
          size="medium"
        >
          Secondary
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          size="medium"
          variant="text"
          disabled
        >
          Disabled
        </Button>
      </div>
    </div>
  </Story>
</Preview>

### Small buttons

<Preview>
  <Story name="Small" decorators={[withKnobs, withA11y]}>
    <div>
      <div className="row-items">
        <Button isLoading={knobButtonLoading()} size="small">
          Default
        </Button>
        <Button isLoading={knobButtonLoading()} color="primary" size="small">
          Primary
        </Button>
        <Button isLoading={knobButtonLoading()} color="secondary" size="small">
          Secondary
        </Button>
        <Button isLoading={knobButtonLoading()} size="small" disabled>
          Disabled
        </Button>
      </div>
      <div className="row-items">
        <Button isLoading={knobButtonLoading()} size="small" variant="outlined">
          Default
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="primary"
          variant="outlined"
          size="small"
        >
          Primary
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="secondary"
          variant="outlined"
          size="small"
        >
          Secondary
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          size="small"
          variant="outlined"
          disabled
        >
          Disabled
        </Button>
      </div>
      <div className="row-items">
        <Button isLoading={knobButtonLoading()} size="small" variant="text">
          Default
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="primary"
          variant="text"
          size="small"
        >
          Primary
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          color="secondary"
          variant="text"
          size="small"
        >
          Secondary
        </Button>
        <Button
          isLoading={knobButtonLoading()}
          size="small"
          variant="text"
          disabled
        >
          Disabled
        </Button>
      </div>
    </div>
  </Story>
</Preview>

## Icons

<Preview>
  <Story name="Icons" decorators={[withKnobs, withA11y]}>
    <div className="row-items">
      <Button isLoading={knobButtonLoading()} startIcon="←" variant="outlined">
        With icon
      </Button>
      <Button isLoading={knobButtonLoading()} endIcon="→" variant="outlined">
        With icon
      </Button>
    </div>
  </Story>
</Preview>

## Loading state

<Preview>
  <Story name="Loading state" decorators={[withKnobs, withA11y]}>
    <div className="row-items">
      <Button isLoading={true} color="primary">
        Loading example
      </Button>
      <Button isLoading={true} color="secondary" size="large">
        Loading example
      </Button>
    </div>
  </Story>
</Preview>

# Props

<Props of={Button} />
