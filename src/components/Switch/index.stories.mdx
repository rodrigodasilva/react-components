import {
  Meta,
  Story,
  Description,
  Preview,
  Props,
} from '@storybook/addon-docs/blocks';
import { withKnobs } from '@storybook/addon-knobs';
import { withA11y } from '@storybook/addon-a11y';
import { FaGithub } from 'react-icons/fa';

import Switch from '.';

<Meta title="Components/Switch" component={Switch} />

# Switch

## Base

<Preview>
  <Story name="Base" decorators={[withKnobs, withA11y]}>
    <form
      className="row-items"
      style={{ display: 'flex', justifyContent: 'space-between' }}
    >
      <Switch label="Default" />
      <Switch label="Checked" />
      <Switch label="Disabled" disabled />
    </form>
  </Story>
</Preview>

## Color

<Preview>
  <Story name="Color" decorators={[withKnobs, withA11y]}>
    <form
      className="row-items"
      style={{ display: 'flex', justifyContent: 'space-between' }}
    >
      <Switch label="Primary" color="primary" />
      <Switch label="Success" color="success" />
      <Switch label="Danger" color="danger" />
    </form>
  </Story>
</Preview>

## Label placement

<Preview>
  <Story name="Label placement" decorators={[withKnobs, withA11y]}>
    <form
      className="row-items"
      style={{ display: 'flex', justifyContent: 'space-between' }}
    >
      <Switch label="Start" labelPlacement="start" />
      <Switch label="End" />
      <Switch label="Top" labelPlacement="top" />
      <Switch label="Bottom" labelPlacement="bottom" />
    </form>
  </Story>
</Preview>

## Validation

<Preview>
  <Story name="Validation" decorators={[withKnobs, withA11y]}>
    <form
      className="row-items"
      style={{ display: 'flex', justifyContent: 'space-between' }}
    >
      <Switch label="Start" labelPlacement="start" error="Erro de validação" />
      <Switch label="End" error="Erro de validação" />
      <Switch label="Top" labelPlacement="top" error="Erro de validação" />
      <Switch
        label="Bottom"
        labelPlacement="bottom"
        error="Erro de validação"
      />
    </form>
  </Story>
</Preview>

# Props

<Props of={Switch} />
