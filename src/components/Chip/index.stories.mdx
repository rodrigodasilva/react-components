import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, boolean, select } from '@storybook/addon-knobs';
import { withA11y } from '@storybook/addon-a11y';

import {
  knobButtonLoading,
  knobButtonSizes,
} from '../../../.storybook/knobs/button';

import Chip from '.';

<Meta title="Components/Chip" component={Chip} />

# Chip

## Variants

### Filled

<Preview>
  <Story name="Filled" decorators={[withKnobs, withA11y]}>
    <div style={{ display: 'flex', justifyContent: 'space-between' }}>
      <Chip label="default" />
      <Chip label="primary" color="primary" />
      <Chip label="secondary" color="secondary" />
      <Chip label="success" color="success" />
      <Chip label="danger" color="danger" />
    </div>
  </Story>
</Preview>

### Outlined

<Preview>
  <Story name="Outlined" decorators={[withKnobs, withA11y]}>
    <>
      <div style={{ display: 'flex', justifyContent: 'space-between' }}>
        <Chip label="default" variant="outlined" />
        <Chip label="primary" color="primary" variant="outlined" />
        <Chip label="secondary" color="secondary" variant="outlined" />
        <Chip label="success" color="success" variant="outlined" />
        <Chip label="danger" color="danger" variant="outlined" />
      </div>
    </>
  </Story>
</Preview>

## Size

### Small

<Preview>
  <Story name="Small" decorators={[withKnobs, withA11y]}>
    <>
      <div style={{ display: 'flex', justifyContent: 'space-between' }}>
        <Chip label="default" size="small" />
        <Chip label="primary" color="primary" variant="outlined" size="small" />
        <Chip label="secondary" color="secondary" size="small" />
        <Chip label="success" color="success" variant="outlined" size="small" />
        <Chip label="danger" color="danger" size="small" />
      </div>
    </>
  </Story>
</Preview>

## On delete

<Preview>
  <Story name="onDelete" decorators={[withKnobs, withA11y]}>
    <>
      <div style={{ display: 'flex', justifyContent: 'space-between' }}>
        <Chip label="default" size="small" onDelete={() => alert('onDelete')} />
        <Chip
          label="primary"
          variant="outlined"
          color="primary"
          size="small"
          onDelete={() => alert('onDelete')}
        />
        <Chip
          label="secondary"
          color="secondary"
          onDelete={() => alert('onDelete')}
        />
        <Chip
          label="success"
          color="success"
          variant="outlined"
          onDelete={() => alert('onDelete')}
        />
        <Chip
          label="danger"
          color="danger"
          onDelete={() => alert('onDelete')}
        />
      </div>
    </>
  </Story>
</Preview>

# Props

<Props of={Chip} />
