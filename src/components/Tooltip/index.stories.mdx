import {
  Meta,
  Story,
  Description,
  Preview,
  Props,
} from '@storybook/addon-docs/blocks';
import { withKnobs } from '@storybook/addon-knobs';
import { withA11y } from '@storybook/addon-a11y';

import { knobSpinnerSize } from '../../../.storybook/knobs/spinner';

import Button from '../Button';
import Tooltip from '.';

<Meta title="Components/Tooltip" component={Tooltip} />

# Tooltip

## Theme

<Preview>
  <Story name="Theme" decorators={[withKnobs, withA11y]}>
    <div
      className="row-items align-center lg-offset-top"
      style={{ minHeight: 150, justifyContent: 'space-around' }}
    >
      <Tooltip
        theme="dark"
        render={() => <span>Lorem, ipsum dolor sit amet consectetur.</span>}
      >
        <button type="button">Dark</button>
      </Tooltip>
      <Tooltip
        render={() => <span>Lorem, ipsum dolor sit amet consectetur.</span>}
      >
        <button type="button">Light</button>
      </Tooltip>
    </div>
  </Story>
</Preview>

## Position

<Preview>
  <Story name="Position" decorators={[withKnobs, withA11y]}>
    <div
      className="row-items align-center lg-offset-top"
      style={{ minHeight: 150, justifyContent: 'space-around' }}
    >
      <Tooltip
        theme="dark"
        render={() => <span>Lorem, ipsum dolor sit amet</span>}
      >
        <button type="button">Top</button>
      </Tooltip>
      <Tooltip
        position="right"
        render={() => <span>Lorem, ipsum dolor sit amet</span>}
      >
        <button type="button">Right</button>
      </Tooltip>
      <Tooltip
        theme="dark"
        position="bottom"
        render={() => <span>Lorem, ipsum dolor sit amet</span>}
      >
        <button type="button">Bottom</button>
      </Tooltip>
      <Tooltip
        position="left"
        render={() => <span>Lorem, ipsum dolor sit amet</span>}
      >
        <button type="button">Left</button>
      </Tooltip>
    </div>
  </Story>
</Preview>

# Props

<Props of={Tooltip} />
